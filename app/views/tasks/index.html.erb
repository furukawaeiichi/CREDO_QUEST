<div class="container px-4 py-5 my-5">
  <div class="text-center">

    <p style="color: white"><%= notice %></p>

    <h1 class="white-text">みんなのクエスト</h1>

    <%= link_to "マイクエスト作成", new_task_path, class: "btn btn-primary", data: { turbo_frame: dom_id(Task.new) } %>
    <%= turbo_frame_tag Task.new %>
    <div id="tasks">
      <%= render @tasks %>
    </div>

  </div>
</div>
